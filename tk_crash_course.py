from tkinter import *
from tkinter.ttk import *

# Create window named as root
root = Tk()

# Wiriting title to the main window
root.title("First Programe")

# Label to show output in window
label = Label(root, text="Hello World")

# To add button 

# Calling mainloop, it will take the program in loop, so that window will not destroy
root.mainloop()